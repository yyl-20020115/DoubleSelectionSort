#pragma once
int FastSum256(int data[], size_t size);
long long FastSum256(long long data[], size_t size);

unsigned int FastSum256(unsigned int data[], size_t size);
unsigned long long FastSum256(unsigned long long data[], size_t size);

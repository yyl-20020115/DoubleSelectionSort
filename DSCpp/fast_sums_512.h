#pragma once
int FastSum512(int data[], size_t size);
long long FastSum512(long long data[], size_t size);

unsigned int FastSum512(unsigned int data[], size_t size);
unsigned long long FastSum512(unsigned long long data[], size_t size);
